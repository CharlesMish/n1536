{
  "name": "n1536",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "serve:dist": "node scripts/serve-dist.mjs",
    "test": "node --test",
    "check:csp": "node scripts/check-build.mjs",
    "validate": "npm test && npm run build && npm run check:csp"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "wrangler": "^4.0.0"
  }
}
